# Add version Control
* `$ git init`
* `$ touch .gitignore

`.gitignore`

```
node_modules
```

* `$ git add -A`
* `$ git commit -m`
* `$ hub create`
    - `$ brew install hub` (if you don't have it installed)
    - Set up your SSH keys for github
