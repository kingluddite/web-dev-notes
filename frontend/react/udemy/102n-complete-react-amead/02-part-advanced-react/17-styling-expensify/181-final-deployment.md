# Final Deployment
```
# $ git status
$ gs

# $ git add -A
$ ga .

# $ git status
$ gs

# clear the terminal
$ clear

# $ git commit -m "atomic commit message"
$ gc -m 'Add Expensified styles'

# $ git push origin master (to GitHub)
$ gpush

# $ git push heroku master (to Heroku production server)
$ gph
```

